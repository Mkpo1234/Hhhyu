services:
  - type: web
    name: telegram-bot
    env: python
    repo: https://github.com/اسم_مستودعك_هنا
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
